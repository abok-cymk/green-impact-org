{
  "framework": "vite",
  "buildCommand": "pnpm run build",
  "outputDirectory": "dist",
  "cleanUrls": true,
  "rewrites": [{ "source": "/api/(.*)", "destination": "/api/$1" }],
  "functions": { "api/**/*.{ts,tsx}": { "maxDuration": 15,  "includeFiles": "api/**" } }
}
